// Package imports:
import 'package:kurumi/cupertino.dart';

class DownloaderScope extends StatelessWidget {
  const DownloaderScope({
    required this.child,
    super.key,
  });

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
